name: Build and Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Maven Build
      - uses: ./.github/actions/maven-build
        with:
          java-version: '21'